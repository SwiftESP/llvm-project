## Autogenerated from //clang/test/Unit/lit.site.cfg.py.in, do not edit

# Takes a path that's relative to __file__ and makes it absolute.
def path(p):
    return os.path.abspath(os.path.join(__file__, '..', p)) if p else ''

import sys

config.llvm_src_root = path(r"../../../../../../llvm")
config.llvm_obj_root = path(r"../../../../obj/llvm")
config.llvm_tools_dir = lit_config.substitute(path(r"../../../../bin"))
config.llvm_libs_dir = lit_config.substitute(path(r""))
config.llvm_build_mode = lit_config.substitute(".")
config.clang_obj_root = path(r"../../../../obj/clang")
config.enable_shared = 0
config.shlibdir = lit_config.substitute(path(r"../../../../lib"))
config.target_triple = "arm64-apple-darwin"

# Let the main config do the real work.
lit_config.load_config(
    config, os.path.join(path(r"../../../../../../clang"), "test/Unit/lit.cfg.py"))
